import Head from 'next/head';
import styled from 'styled-components';

export default function Servicios() {
	return (
		<>
			<Head>
				<title>Transparenta | Servicios</title>
			</Head>
			<main className="container mx-auto flex items-center justify-center"></main>
		</>
	);
}
